---
title: "Hello, world"
summary: "PLACEHOLDER — replace with a real first post, or delete this file."
date: "2026-06-01"
tags: ["meta"]
draft: true
---

This is a placeholder post so the blog templates have something to render. It's marked
`draft: true` in the frontmatter, so it's excluded from `getAllPosts()` by default — set
`draft: false` (or delete this file) once you have a real post.

Posts live in `content/blog/*.mdx`. Add a new file with frontmatter (`title`, `summary`, `date`,
optional `tags`) and the post body in Markdown/MDX below it — no other wiring needed.
